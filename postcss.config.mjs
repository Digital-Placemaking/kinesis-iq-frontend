/**
 * postcss.config.mjs
 * PostCSS configuration file.
 * Configures Tailwind CSS plugin for CSS processing.
 */

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
